{
  "name": "myseizures",
  "version": "0.1.0",
  "private": true,
  "description": "Academic offline-first epilepsy care prototype",
  "workspaces": [
    "mobile",
    "api"
  ],
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "start": "npm run start --workspace mobile",
    "dev:api": "npm run dev --workspace api",
    "test": "npm run test --workspaces --if-present"
  }
}
