# Mobile clients must use a network-reachable address when running on a device.
EXPO_PUBLIC_API_BASE_URL=http://10.0.2.2:3000

# Simulated REST API runtime configuration. Replace the placeholder locally; never commit a real secret.
API_PORT=3000
API_TOKEN_SECRET=replace-with-a-long-local-development-secret
